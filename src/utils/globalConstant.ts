// Purpose: button type and shift status constants.
export const ButtonType = {
  SUCCESS: 'success',
  FAIL: 'fail',
  DISABLED: 'disabled',
};

// Purpose: shift status constants.
export const ShiftStatus ={
  OVERLAPPING: 'Overlapping',
  BOOKED: 'Booked',
}

// Purpose: export all constants as a single object.
export default{
  ButtonType,
  ShiftStatus
}